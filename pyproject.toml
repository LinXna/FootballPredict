[project]
name = "footballpredict"
version = "2.0.0"
description = "Statistical + Live Football Prediction System"
requires-python = ">=3.10"

dependencies = [
    "numpy>=1.26",
    "pandas>=2.1",
    "scipy>=1.11",
]

[tool.black]
line-length = 88

[tool.pytest.ini_options]
testpaths = ["tests"]